import { defineApp } from "convex/server";
// // import aggregate from "@convex-dev/aggregate/convex.config";

const app = defineApp();
// app.use(aggregate, { name: "ostQuestions" }); // имя должно совпадать с тем, что вы используете в коде
export default app;
